# Synthetic samples

Only synthetic shipment and event files belong here. Do not copy or anonymize a real company export and commit it as a sample.

Each sample should state:

- the situation it represents;
- whether the import should pass or fail;
- which validation errors are expected;
- which rule should create an exception, if any.

CSV and XLSX versions should describe the same input contract whenever possible.
