# Source code

Application code will be added after the first system-design slice is understood. The initial implementation should keep file ingestion, job processing, domain rules, persistence, API code, and UI code easy to locate and test.
