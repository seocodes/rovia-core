# Tests

Tests will start from synthetic files and observable behavior. The first checks should cover validation, duplicate imports, event ordering, deterministic rules, queue retries, and operator decisions.
